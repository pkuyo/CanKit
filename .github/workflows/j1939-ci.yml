name: CI-j1939

on:
  push:
    paths:
      - 'src/protocols/CanKit.Pro.J1939/**'
      - 'src/transports/CanKit.Pro.J1939Tp/**'
      - 'src/core/CanKit.Abstractions/**'
      - 'src/core/CanKit.Core/**'
      - 'src/core/CanKit.Pro.Actor/**'
      - 'src/core/CanKit.Pro.Addressing/**'
      - 'src/core/CanKit.Pro.RawCan/**'
      - 'src/core/CanKit.Pro.Reliability/**'
      - 'src/adapters/CanKit.Adapter.Virtual/**'
      - 'tests/CanKit.Tests/**'
      - 'CanKitProJ1939.slnf'
      - '.github/workflows/j1939-ci.yml'
  pull_request:
    paths:
      - 'src/protocols/CanKit.Pro.J1939/**'
      - 'src/transports/CanKit.Pro.J1939Tp/**'
      - 'src/core/CanKit.Abstractions/**'
      - 'src/core/CanKit.Core/**'
      - 'src/core/CanKit.Pro.Actor/**'
      - 'src/core/CanKit.Pro.Addressing/**'
      - 'src/core/CanKit.Pro.RawCan/**'
      - 'src/core/CanKit.Pro.Reliability/**'
      - 'src/adapters/CanKit.Adapter.Virtual/**'
      - 'tests/CanKit.Tests/**'
      - 'CanKitProJ1939.slnf'
      - '.github/workflows/j1939-ci.yml'

jobs:
  linux:
    name: J1939 (Ubuntu)
    runs-on: ubuntu-latest
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Configure endpoints
        run: |
          set -e
          echo "CANKIT_TEST_ADAPTERS=CanKit.Adapter.Virtual" >> "$GITHUB_ENV"

      - name: Restore
        run: dotnet restore CanKitProJ1939.slnf

      - name: Test (net8.0)
        run: dotnet test CanKitProJ1939.slnf -c Release -f net8.0 --logger "trx;LogFileName=TestResults.trx"

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-ubuntu
          path: '**/TestResults.trx'

  windows:
    name: J1939 (windows) – ${{ matrix.tfm }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        tfm: [net8.0, net48]
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Configure endpoints
        shell: pwsh
        run: |
          "CANKIT_TEST_ADAPTERS=CanKit.Adapter.Virtual" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf8 -Append

      - name: Restore
        run: dotnet restore CanKitProJ1939.slnf

      - name: Test (${{ matrix.tfm }})
        run: dotnet test CanKitProJ1939.slnf -c Release -f ${{ matrix.tfm }} --logger "trx;LogFileName=TestResults-${{ matrix.tfm }}.trx"

      - name: Upload Test Results (${{ matrix.tfm }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-windows-${{ matrix.tfm }}
          path: '**/TestResults-${{ matrix.tfm }}.trx'
