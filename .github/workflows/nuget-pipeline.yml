# TEMPORARILY DISABLED (2026-07-16): NuGet pack/publish is paused and will be
# re-enabled later. Keep the workflow file so history/path filters stay intact.
# Original triggers (pull_request / push to main / tags / workflow_dispatch) are
# commented out below — only a no-op manual dispatch remains so the file stays valid.

name: NuGet pipeline

# on:
#   pull_request:
#     paths:
#       - "src/**"
#       - "tests/**"
#       - "eng/**"
#       - "CHANGELOG.md"
#       - ".github/workflows/nuget-pipeline.yml"
#   push:
#     branches:
#       - main
#     tags:
#       - "v*"
#     paths:
#       - "src/**"
#       - "tests/**"
#       - "eng/**"
#       - "CHANGELOG.md"
#       - ".github/workflows/nuget-pipeline.yml"
#   workflow_dispatch:

on:
  workflow_dispatch:
    inputs:
      note:
        description: "NuGet pipeline is temporarily disabled; this dispatch is a no-op placeholder."
        required: false
        default: "disabled"

concurrency:
  group: nuget-pipeline-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: NuGet pipeline disabled
        run: |
          echo "NuGet pack/publish is temporarily disabled (2026-07-16)."
          echo "Re-enable by restoring the commented triggers/jobs in this workflow."
