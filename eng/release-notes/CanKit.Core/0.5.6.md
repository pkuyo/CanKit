## 0.5.6

### Changed

* Reworked Windows precision delays to use safe wait handles, respond directly to cancellation, and fall back when high-resolution waitable timers are unavailable.

### Fixed

* Synchronized concurrent registration and lookup in `CanRegistry`, with stable snapshots for endpoint enumeration and factory error reporting.

### Breaking Changes

* None.
