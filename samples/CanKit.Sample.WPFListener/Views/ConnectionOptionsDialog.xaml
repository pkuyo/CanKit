<Window x:Class="EndpointListenerWpf.Views.ConnectionOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EndpointListenerWpf.Views"
        mc:Ignorable="d"
        Title="Connection Options" Height="260" Width="520"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CanKit.Sample.WPFListener;component/Resources/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Mode selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Protocol:" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <RadioButton x:Name="Can20Radio" Content="CAN 2.0" Margin="0,0,12,0"/>
            <RadioButton x:Name="CanFdRadio" Content="CAN FD"/>
        </StackPanel>

        <!-- Bitrates -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Bit Rate:" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <ComboBox x:Name="BitrateCombo" Grid.Column="1">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource BitrateConverter}, ConverterParameter=0.##}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock x:Name="DataBitrateLabel" Grid.Column="3" Text="Data Bit Rate:" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <ComboBox x:Name="DataBitrateCombo" Grid.Column="4">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource BitrateConverter}, ConverterParameter=0.##}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!-- Filters -->
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button x:Name="EditFiltersBtn" Content="Edit Filters..." Width="120" Click="OnEditFilters"/>
            <TextBlock Text="  (Applies when connection starts)" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OnOk"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
